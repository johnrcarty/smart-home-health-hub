import React from "react";

const HistoryModal = ({ onClose }) => {
  return (
    <div className="history-modal">
      {/* History modal body will go here */}
    </div>
  );
};

export default HistoryModal;
